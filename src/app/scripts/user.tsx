// Assuming you have a variable `isLoggedIn` that indicates if the user is logged in or not

function CheckUserLoginStatus() {
    
    
    
    if (isLoggedIn) {
        console.log("User is logged in");
        return true;
    }

    return false
}